import SharedRadioButtonHistory from "@/shared/components/Inputs/RadioButtonHistory";

const RadioButtonHistory = (
  props: Parameters<typeof SharedRadioButtonHistory>[0],
) => <SharedRadioButtonHistory {...props} accentColor="#df2af9" />;

export default RadioButtonHistory;
